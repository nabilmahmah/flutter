import 'package:flutter/material.dart';

class HaveData extends StatelessWidget {
  const HaveData({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("have data")));
  }
}
